# Test Cases — Version Index

This folder will track the Test Cases lineage through its version lifecycle:
**Draft (Pre-Baseline) → Baseline (Pre-CR) → Baseline (Post-CR)**, per
`../../../.claude/rules/docs-storage.md` §4.

No version exists yet. When the first Test Cases document is promoted here
(per §3, Draft → official promotion flow):

1. Create `v1.0/` and place the file inside it.
2. Replace this README with a version-index table matching the pattern in
   [`docs/share/brd/README.md`](../brd/README.md) — version, folder, file,
   status (**Baseline (Pre-CR) — CURRENT**), date.
